section.page-container(ng-class="{ 'swiped': !mainPage.global.isNavMenu }")
  section.left-side
    include menus/_navigation-menu
  //TODO change to directive!!!! rooms will be more
  section.right-side.main-page-bg(ng-class="{'kitchen': mainPage.product.selectedRoomId == 1, 'livingroom': mainPage.product.selectedRoomId == 2, 'balcony': mainPage.product.selectedRoomId == 3, 'childroom': mainPage.product.selectedRoomId == 4, 'bedroom': mainPage.product.selectedRoomId == 5, 'door': mainPage.product.selectedRoomId == 6 }")
    section.main-content
      include parts/_user-info
      include parts/_room-info
      include parts/_room-selector

      include panels/_template-selector
      include panels/_profile
      include panels/_glass
      include panels/_hardware-window
      include panels/_lamination
      include panels/_add-elements
      include panels/_add-elements-list
      .backDarkView(ng-show="mainPage.global.isShowCommentBlock")
    section.right-menu
      include menus/_config-menu
      include menus/_addelements-menu